// Use any of the doGet examples above.
// Then, in the Apps Script editor, select "Deploy" > "New deployment" > "Web app".
function doGet(e) {
  return HtmlService.createHtmlOutput("Your web app is working!");
}

